// const someFunc = () => {
//   console.log('ya ya');
// };

// export const debounce = (func) => () => {
//   setTimeout(func, 500);
// };

// const debounceSomeFunc = debounce(someFunc);

// debounceSomeFunc();
